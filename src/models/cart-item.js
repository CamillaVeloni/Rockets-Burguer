class CartItem {
    constructor(foodTitle, foodImage, foodPrice, quantity, sum) {
        this.foodTitle = foodTitle;
        this.foodImage = foodImage;
        this.foodPrice = foodPrice;
        this.quantity = quantity;
        this.sum = sum;
    }
}

export default CartItem;