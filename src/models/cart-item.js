class CartItem {
    constructor(foodTitle, foodPrice, quantity, sum) {
        this.foodTitle = foodTitle;
        this.foodPrice = foodPrice;
        this.quantity = quantity;
        this.sum = sum;
    }
}

export default CartItem;